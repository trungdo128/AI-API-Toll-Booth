{
  "name": "ai-api-toll-booth",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test && node --test apps/web/*.test.js",
    "test:contracts": "cargo test --manifest-path packages/contracts/Cargo.toml",
    "start": "pnpm --filter @toll-booth/api start",
    "migrate": "pnpm --filter @toll-booth/api migrate",
    "seed": "pnpm --filter @toll-booth/api seed"
  }
}
