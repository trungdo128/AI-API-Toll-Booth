# Testing

CI runs JavaScript unit/integration tests, strict TypeScript, production builds, Playwright desktop/mobile tests, dependency audit, secret scanning, Rust formatting, clippy, contract tests and the optimized-target contract build.

Real Testnet smoke evidence is separate from automated tests. A flow is recorded only after its explorer transaction and current deployed commit are verified.
