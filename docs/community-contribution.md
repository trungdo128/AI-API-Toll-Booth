# Community contribution

The reusable HTTP 402 challenge/receipt client lives in `apps/agent-client`. Publication as an independent package and a real package/repository URL remain pending; no external link is claimed.
