# Community contribution

The reusable HTTP 402 challenge/receipt client lives in `apps/agent-client`. It documents challenge parsing, exact-payment signing, receipt submission and protected-request retry for Stellar integrations.
