# Product overview

AI API Toll Booth is a Testnet marketplace for per-request and funded-session API access. Providers publish approved plans while encrypted upstream configuration stays server-side. Consumers receive a machine-readable HTTP 402 challenge and retry only after backend transaction verification.
